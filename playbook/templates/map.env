SERVER_MAP={{ item.map }}
MAP_NAME={{ item.name | default(item.map) | regex_replace('_WP', '') }}
MAP_ID={{ map }}
SESSION_NAME={{ asa_cluster_id }} {{ item.name | default(item.map) | regex_replace('_WP', '') }}
SERVER_PORT={{ 7790 + idx }}
RCON_PORT={{ 32330 + idx }}
